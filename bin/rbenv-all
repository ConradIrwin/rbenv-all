#!/usr/bin/env bash
set -e
[ -n "$RBENV_DEBUG" ] && set -x

bare=0

if [ "$1" = "--bare" ]; then
    bare=1
    shift
fi

eval "$(rbenv init -)"

for v in $(rbenv versions --bare); do
    [ "$bare" != "1" ] && echo "$v>> $*" 1>&2
    rbenv shell $v
    $*
    echo
done
